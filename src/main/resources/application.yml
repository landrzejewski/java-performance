spring:
  datasource:
    username: admin
    password: admin
    url: jdbc:postgresql://postgres:5432/reports
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true

# Total time: 195815ms vs 328280ms
# Start time:  4.315s vs 0.414s

# ./mvnw -Pnative spring-boot:build-image
